import Default from './avatar';
import AvatarSize from './avatar-size';

import type { ComponentDemo } from '../../interface';

const previewerDemo: ComponentDemo[] = [Default, AvatarSize];

export default previewerDemo;
