import Default from './radio';
import Button from './button';

import type { ComponentDemo } from '../../interface';

const previewerDemo: ComponentDemo[] = [Default, Button];

export default previewerDemo;
