import Default from './card';
import inner from './inner';

import type { ComponentDemo } from '../../interface';

const previewerDemo: ComponentDemo[] = [Default, inner];

export default previewerDemo;
