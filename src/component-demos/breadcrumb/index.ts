import Default from './breadcrumb';
import Icons from './breadcrumb-icons';

import type { ComponentDemo } from '../../interface';

const previewerDemo: ComponentDemo[] = [Default, Icons];

export default previewerDemo;
